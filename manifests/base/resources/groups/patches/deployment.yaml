apiVersion: apps/v1
kind: Deployment
metadata:
  name: groups
spec:
  template:
    spec:
      containers:
        - name: main
          env:
            - $patch: delete
              name: KEYS_URI
            - name: KEYS_URI_TEMP
              valueFrom:
                configMapKeyRef:
                  key: keysUri
                  name: groups-configs
            - name: KEYS_URI
              value: $(KEYS_URI_TEMP)/keys/qlik.api.internal
