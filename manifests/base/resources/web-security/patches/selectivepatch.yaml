apiVersion: qlik.com/v1
kind: SelectivePatch
metadata:
  name: web-security
enabled: true
patches:
  - path: deploymentJSON.yaml
    target:
      kind: Deployment
      name: web-security
  - path: deployment.yaml
