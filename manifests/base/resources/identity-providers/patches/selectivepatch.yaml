apiVersion: qlik.com/v1
kind: SelectivePatch
metadata:
  name: identity-providers
enabled: true
patches:
  - path: deploymentJSON.yaml
    target:
      kind: Deployment
      name: identity-providers
  - path: deployment.yaml
