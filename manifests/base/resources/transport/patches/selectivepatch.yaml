apiVersion: qlik.com/v1
kind: SelectivePatch
metadata:
  name: transport
enabled: true
patches:
  - path: deploymentJSON.yaml
    target:
      kind: Deployment
      name: transport
  - path: deployment.yaml
